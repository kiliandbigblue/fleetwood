packages:
  - packages/*

# pnpm 10 blocks postinstall scripts by default. Electron's downloads the actual
# binary, so without this the app cannot start at all.
onlyBuiltDependencies:
  - electron
  - esbuild
